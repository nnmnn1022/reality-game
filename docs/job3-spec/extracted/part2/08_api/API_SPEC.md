# API Specification (Product Level)

POST /experiences
POST /experiences/{id}/join
POST /experiences/{id}/start

POST /events

GET /scene/current

POST /input

GET /ending

API는 Story Beat가 아니라 Scene 중심으로 외부에 노출한다.
